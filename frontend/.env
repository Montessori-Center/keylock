# frontend/.env
DANGEROUSLY_DISABLE_HOST_CHECK=true
PORT=3000
BROWSER=none
HTTPS=false
HOST=127.0.0.1

# ПОЛНОЕ отключение WebSocket
FAST_REFRESH=false
WATCHPACK_POLLING=false

# Не указываем WDS_SOCKET настройки вообще!
# (убираем WDS_SOCKET_HOST, WDS_SOCKET_PORT полностью)

# API URL
REACT_APP_API_URL=/api